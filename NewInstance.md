# NewInstance

To get NewInstance search string "NewInstance", it has 5 xrefs, choose first one and decompile it:
```c
double *__fastcall sub_AB98C0(__int64 a1) // <-- NewInstance
``` 

so the offset is 0xAB98C0
